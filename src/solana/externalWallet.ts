/*
import {
  Account,
  Connection,
  Transaction,
  TransactionInstruction,
} from "@solana/web3.js";

// import Wallet from "@project-serum/sol-wallet-adapter";
// import { targetCluster, COMMITMENT } from "./targetCluster";

//const PROVIDER_URL = "https://www.sollet.io";
// const wallet = new Wallet(PROVIDER_URL, targetCluster);

// const useExternalSignature = async(
//  instructions: TransactionInstruction[],
//  connection: Connection,
//  wallet: Wallet,
// ) => {

// };
const connectWallet = () => {
  if (!wallet.connected) {
    return wallet.connect() as Promise<void>;
  } else {
    return Promise.resolve();
  }
};

const useWallet = async(): Promise<Wallet> => {
  await connectWallet();
  return wallet;
  };
 */
export {};
